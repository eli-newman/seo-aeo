import { log } from "../../engine/log.js";

/** Move a draft into posts/ and commit. Implemented in M3. */
export async function runApprove(_root: string, _slug: string): Promise<void> {
  log.warn("`approve` is not implemented yet (M3).");
}
