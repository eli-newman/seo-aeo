import { log } from "../../engine/log.js";

/** List drafts and posts. Implemented in M3. */
export async function runList(_root: string): Promise<void> {
  log.warn("`list` is not implemented yet (M3).");
}
